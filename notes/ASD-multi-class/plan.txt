1 使用AI4Mars数据集，四个类别，
2 联合探测的原因
    2.1 新颖和目标是两个任务
    2.2 同步探测可以分享视觉特征
    2.3 高效，节约资源
3 
3.1 岩石地物的特点是结构信息很重要，从NASA开发的岩石分类算法来看，基于结构的往往结构较好
3.2 目前的需要捕获全局结构信息，加个attention？
3.3 必须实现直接输出异常分数，防止异常度量指标+归一化受到躁点影像，例子见笔记本火星文件夹的0，1，2图
3.4 直接输出异常分数采用ranking模型的方式，挑选异常样本自训练归一化
3.5 三个分支？ SVDD 头部用于追求“normal的同”，聚类头部用于保持多个normal class的异，
第三个头部用于直接输出异常分数。

放弃火星数据，但基本创新不变，改用森林数据
1 支持了batch size，并在计算svdd loss时采用0.9分位点而非max，增强鲁棒性，但是旋转转置损失保持不变，取一个就行了
2 上次看的知乎关于高维空间问题的文章，讲到维度越高，样本越聚集于边界周围。因此对于ASD模型，可以采用距离半径超球面距离的异常计算方式，
而本次低纬度时候采用距离中心的更优。这个可以作为为什么直接输出异常分数的第二个motivation

森林数据和高分高光谱离得比较远，还是采用卫星/无人机遥感俯视视角的影像，DeepGlobe那个地表覆盖分类的感觉不错。

放在实验讨论环节有意思的一个实验：联合探测新颖和目标，是不是对于没有见过的目标，具备视觉学习能力之后都可以探测，可以测试在火灾数据上的效果

遥感数据很多，负样本是不是可以直接拿来用？比数据增强好的多

直接拿部分训练样本构造伪标签，这样就可以不借助额外的样本了？

直接对异常分数回归，而非0，1回归？

将内部元素聚类分的问题是即使知道类被个数，严重样本不均衡很难分出来。

目前实现了二阶段的联合异常和新颖非监督分割，但是发现，分出来两个类别的探测得分都是和为1的。理论上讲，新颖的得分乘过去是可以辅助探测的。
怀疑是之前训练过程的问题，重新训练试试

发现融合后是降低的，感觉想测试下训练时候就融合


其实很多时候是真实标签标注不合理的问题，比如gravel和sand

目前2345的第二阶段novelty 分数不对，normalid忘记写5了，之后重新测一下

下载的一些结果感觉可视化图和结果以及类别名对不上，一些novelty GT，明明就是已知地物怎么会是novelty？

感觉是不是Over-clustering会好点？

机器认为的novelty和人认为的还是有差别，机器更关注局部细节，而人更关注总体形状这些。